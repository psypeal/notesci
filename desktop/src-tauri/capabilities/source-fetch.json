{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "source-fetch",
  "description": "Legacy source-fetch capability retained only so stale windows can be closed and old preview_bytes events remain harmless. Active source review uses the normal source-preview window plus backend PDF byte fetches.",
  "windows": ["source-fetch"],
  "webviews": ["source-fetch"],
  "remote": {
    "urls": ["http://*/*", "https://*/*"]
  },
  "permissions": ["allow-preview-bytes"]
}
