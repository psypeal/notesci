{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "source-fetch",
  "description": "Hidden loader window that opens an arbitrary remote source page to clear JS/Cloudflare challenges, then hands its bytes/text back via exactly one command — preview_bytes. No other notesci IPC is exposed to the remote page.",
  "windows": ["source-fetch"],
  "webviews": ["source-fetch"],
  "remote": {
    "urls": ["http://*/*", "https://*/*"]
  },
  "permissions": ["allow-preview-bytes"]
}
