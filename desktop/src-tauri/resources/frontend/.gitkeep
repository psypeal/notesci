# Placeholder so bundle.resources ("resources/frontend/" -> "frontend/") validates at build time
# when no frontend tree is staged here (e.g. the .deb build, which stays Mode::System/Installed).
# macOS/Windows/AppImage builds rm -rf this dir and stage the real frontend tree via
# packaging/embedded-python/* (python, backend) or frontend/dist (frontend).
